# Skill extraction is done by the team's own cv_parser/ (vendored from
# Team-ScriptFusion/DevScore — see cv_parser/VENDORED.md). Its requirements are
# a subset of these, so one install covers both.
#
# Everything else (GitHub mining, static analysis, matching, scoring) is
# standard library on purpose: the SDS's non-functional requirements say
# open-source, zero licence cost, and the fewer wheels this needs the easier it
# is to deploy beside the Node backend.
pdfplumber==0.11.9

# HTTP service (service/app.py). Pinned to match cv_parser/requirements.txt
# in the main repository so both Python services deploy identically.
flask==3.1.3
gunicorn==26.0.0

# Optional — OCR fallback for CVs exported as a flattened image with no text
# layer. 2 of the 47 CVs in the collected dataset are in this state: without
# it they parse to zero skills and the candidate scores as if they claimed
# nothing. Also needs the tesseract binary on PATH:
#   Windows  winget install UB-Mannheim.TesseractOCR
#   macOS    brew install tesseract
#   Ubuntu   sudo apt install tesseract-ocr
pytesseract==0.3.13
