requests>=2.31
rapidfuzz>=3.6
python-dotenv>=1.0       # lets run_batch.py read GITHUB_TOKEN from a local .env file
scikit-learn>=1.3        # TfidfEmbeddingProvider (active today, no download needed)
sentence-transformers>=2.5   # SentenceTransformerEmbeddingProvider (spec's design; needs HF network access to run)
pdfplumber>=0.11          # cv_extract.py: PDF text extraction
pypdf>=3.17               # cv_extract.py: PDF link-annotation scanning (GitHub URLs not in visible text)
pdf2image>=1.17           # cv_extract.py: renders scanned pages to images for OCR fallback
pytesseract>=0.3.10       # cv_extract.py: OCR fallback for the 3 scanned CVs (needs Tesseract installed, see README)
pytest>=8.0
pytest-mock>=3.12
